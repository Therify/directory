export const enum FirebaseIntent {
    AddListener = 'add-listener',
    AuthenticateWithCustomToken = 'authenticate-with-custom-token',
    PushData = 'push-data',
    ReadData = 'read-data',
    SetData = 'set-data',
    Signout = 'signout',
    UpdateData = 'update-data',
}
