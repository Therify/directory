import * as COUNTRY from '../country';
import * as STATE from './state';

export const UNITED_STATES = {
    STATE,
    COUNTRY: {
        CODE: COUNTRY.MAP.UNITED_STATES,
        NAME: 'United States',
    },
};
