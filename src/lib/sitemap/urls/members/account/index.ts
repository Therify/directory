import { ACCOUNT_SETTINGS_PATHS } from './account-settings';

export const ACCOUNT_PATHS = {
    SETTINGS: ACCOUNT_SETTINGS_PATHS,
    INVALID_PLAN: '/members/account/invalid-plan',
    BILLING_AND_PAYMENTS: '/members/account/billing',
} as const;
