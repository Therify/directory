import { FOR_EMPLOYERS, FOR_PROVIDERS, ABOUT } from './links';
import { LOGIN } from '../accountLinks';

export const MARKETING_SITE_MENU = [
    FOR_PROVIDERS,
    FOR_EMPLOYERS,
    ABOUT,
    LOGIN,
] as const;
